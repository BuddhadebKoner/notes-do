[phases.setup]
nixPkgs = ["nodejs_18", "npm"]
aptPkgs = ["ca-certificates"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["echo 'No build phase needed for Node.js backend'"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
PORT = "3000"